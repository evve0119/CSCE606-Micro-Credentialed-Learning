const { User, Group, Credential, Resume } = require("./user");
const { Course } = require("./course");
const { Job } = require("./job");
module.exports={
    User,
    Group,
    Credential,
    Course,
    Resume,
    Job,
}
